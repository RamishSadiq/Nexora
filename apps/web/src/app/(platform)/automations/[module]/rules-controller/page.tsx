"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, Plus, Trash2 } from "lucide-react";
export default function RulesControllerPage({ params }: { params: Promise<{ module: string }> }) {
  const [module, setModule] = useState(""); const [rules, setRules] = useState<{source:string; fn:string; target:string}[]>([]); const [source, setSource] = useState("Date of birth"); const [fn, setFn] = useState("Calculate age"); const [target, setTarget] = useState("Age"); const [saved, setSaved] = useState(false);
  useEffect(() => { void params.then(p => { setModule(p.module); setRules(JSON.parse(localStorage.getItem(`nexora-rules-${p.module}`) ?? "[]")); }); }, [params]);
  if (!module) return null; const name = module.replaceAll("-", " ").replace(/\b\w/g, c => c.toUpperCase());
  return <main className="page-enter mx-auto max-w-4xl space-y-6 p-5 text-slate-800 sm:p-8"><Link href={`/automations/${module}`} className="inline-flex items-center gap-2 text-sm font-medium text-indigo-700 hover:underline"><ArrowLeft className="size-4" />{name} automations</Link><header><p className="text-xs font-semibold uppercase tracking-[0.16em] text-indigo-600">Rules controller</p><h1 className="mt-2 text-3xl font-semibold tracking-tight text-slate-950">Create {name} rules</h1><p className="mt-2 text-sm text-slate-600">When a source field changes, apply a function and populate a target field automatically.</p></header><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Add rule</h2><div className="mt-4 grid gap-3 md:grid-cols-[1fr_auto_1fr_auto] md:items-end"><label className="text-sm font-medium">Source field<select className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" value={source} onChange={e => setSource(e.target.value)}><option>Date of birth</option><option>Status</option><option>Created date</option><option>Membership end date</option></select></label><select aria-label="Function" className="rounded-lg border border-slate-300 px-3 py-2" value={fn} onChange={e => setFn(e.target.value)}><option>Calculate age</option><option>Add</option><option>Subtract</option><option>Equals</option><option>Not equal</option><option>Contains</option><option>Set status</option></select><label className="text-sm font-medium">Target field<select className="mt-1 w-full rounded-lg border border-slate-300 px-3 py-2" value={target} onChange={e => setTarget(e.target.value)}><option>Age</option><option>Status</option><option>Category</option><option>Next review date</option></select></label><button className="inline-flex items-center justify-center gap-2 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white" onClick={() => { setRules([...rules, { source, fn, target }]); setSaved(false); }}><Plus className="size-4" />Add rule</button></div></section><section className="rounded-xl border border-slate-200 bg-white p-5"><h2 className="font-semibold">Configured rules</h2><ul className="mt-4 divide-y divide-slate-100">{rules.length === 0 && <li className="py-4 text-sm text-slate-500">No rules yet. Add the DOB calculation above to populate Age automatically.</li>}{rules.map((rule, i) => <li key={i} className="flex items-center justify-between gap-3 py-3 text-sm"><span><strong>{rule.source}</strong><span className="mx-2 text-indigo-600">→ {rule.fn} →</span><strong>{rule.target}</strong></span><button className="text-rose-600" aria-label="Remove rule" onClick={() => { setRules(rules.filter((_, index) => index !== i)); setSaved(false); }}><Trash2 className="size-4" /></button></li>)}</ul><div className="mt-5 flex justify-end gap-3"><button className="rounded-lg border border-slate-300 px-4 py-2 text-sm" onClick={() => setRules(JSON.parse(localStorage.getItem(`nexora-rules-${module}`) ?? "[]"))}>Cancel</button><button className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white" onClick={() => { localStorage.setItem(`nexora-rules-${module}`, JSON.stringify(rules)); setSaved(true); }}>{saved ? "Changes saved" : "Save rules"}</button></div></section></main>;
}
